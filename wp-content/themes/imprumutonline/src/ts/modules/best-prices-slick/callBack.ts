/*let $slick: JQuery,
  $track: JQuery,
  $current: JQuery,
  $active: JQuery;*/

export let init = (event: any): void => {
  /*$slick = $(event.currentTarget);
  $track = $('.slick-track', $slick);
  $current = $('.slick-current', $track);
  $active = $('.slick-active:not(.slick-current)', $track);*/
};

export let beforeChange = (event: any): void => {
  // console.info('beforeChange');
};

export let afterChange = (event: any): void => {
  // console.info('afterChange');
};

export let swipe = (event: any): void => {
  // console.info('swipe');
};

export let breakpoint = (event: any): void => {
  // console.info('breakpoint');
};